# Render Production Environment Variables
# Copy these to your Render dashboard environment variables

# Server Configuration
NODE_ENV=production
PORT=10000
HOST=0.0.0.0

# Database (MongoDB Atlas - get connection string from Atlas dashboard)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ai-career-portal?retryWrites=true&w=majority

# JWT Configuration (Generate a strong secret)
JWT_SECRET=your-super-secure-production-jwt-secret-at-least-256-bits-long
JWT_EXPIRE=24h

# Frontend URL (Will be provided by Render after frontend deployment)
FRONTEND_URL=https://your-frontend-app-name.onrender.com

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=/tmp/uploads
ALLOWED_FILE_TYPES=pdf,doc,docx

# AI Services
GEMINI_API_KEY=your-production-gemini-api-key
OPENAI_API_KEY=your-production-openai-api-key
HUGGINGFACE_API_KEY=your-production-huggingface-api-key

# Email Service (Optional - use SendGrid)
EMAIL_SERVICE=sendgrid
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@your-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# Security
BCRYPT_ROUNDS=12
TRUST_PROXY=true

# Logging
LOG_LEVEL=info

# Database Connection Pool
DB_MIN_POOL_SIZE=2
DB_MAX_POOL_SIZE=10
DB_CONNECTION_TIMEOUT=30000